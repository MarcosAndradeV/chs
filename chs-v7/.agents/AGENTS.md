# CHS compiler

## 1. Technical Memory & Living Context (Obsidian Vault in `docs/`)
**Global AI Behavior Rule:**
- Before proposing complex or architectural changes, consult the `docs/Troubleshooting/` and `docs/Decisions/` folders.
- Whenever you resolve a critical bug or make an architectural decision with the user, create or update the corresponding note within `docs/` using Obsidian reference links (`[[...]]`).

## 2. Avoid `unsafe` blocks
