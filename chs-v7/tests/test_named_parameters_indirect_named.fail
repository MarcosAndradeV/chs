module main

type fptr fn(x: int) -> int

fn foo(x: int) -> int {
    return x;
}

fn main() {
    var f: fptr = foo;
    // Fails because named parameters are not supported in indirect calls
    var res = f(x: 10);
}
